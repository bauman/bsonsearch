#!/usr/bin/python
from setuptools import setup, find_packages

setup(
    name = "python-bsonsearch",
    version = "0.1",
    maintainer = "Dan Bauman",
    maintainer_email = "baumand@live.com",
    packages=find_packages())