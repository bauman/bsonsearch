sudo: required

dist: bionic


language: c


install:
  - sudo apt-get install -y tree libpcre3-dev libpcre3 tree libbson libstemmer0d libstemmer-dev libsodium-dev libaspell-dev aspell aspell-en libsodium23 libbson-dev uthash-dev libsodium-dev yara yara-dev



script:
  - sudo mkdir /usr/include/libbson-1.0/bson && cp /usr/include/libbson-1.0/*.h /usr/include/libbson-1.0/bson/.
  - cmake .
  - make
  - tree


